// Build version — shown on the title screen and stamped into every telemetry session.
// Bump on any player-visible change (see CLAUDE.md conventions).
// (v0.153's auto-deploy push event was missed by GitHub; re-triggered via this no-op merge.)
export const GAME_VERSION = "v0.153";
